{
  "name": "bleep-bloop-api",
  "version": "1.0.0",
  "description": "Bleep Bloop API",
  "main": "src/server.ts",
  "scripts": {
    "build": "cross-env tsc",
    "start": "cross-env NODE_ENV=production node dist/server.js",
    "start-local": "cross-env NODE_ENV=local-test nodemon --watch src --exec ts-node src/server.ts",
    "start-dev": "cross-env NODE_ENV=dev-testing nodemon --watch src --exec ts-node src/server.ts",
    "package-audit": "npm-audit --low=50 --moderate=50 --high=10",
    "test": "jest --config jest.config.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/config": "^3.3.5",
    "@types/jest": "^30.0.0",
    "@types/mssql": "^9.1.7",
    "@types/node": "^22.15.17",
    "@types/nodemailer": "^8.0.1",
    "cross-env": "^10.0.0",
    "jest": "^30.4.2",
    "nodemon": "^3.1.10",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "axios": "^1.9.0",
    "config": "^3.3.12",
    "dotenv": "^17.2.1",
    "fastify": "^5.3.2",
    "mssql": "^11.0.1",
    "nodemailer": "^9.0.3"
  }
}
